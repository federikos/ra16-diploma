import React from 'react';
import PropTypes from 'prop-types';

const Contacts = props => {
  return (
    <div>
      Contacts
    </div>
  );
};

Contacts.propTypes = {
  
};

export default Contacts;